import React, {FC, ReactElement} from 'react';
import AppRoute from "../routes";

/**
 * App component.
 * @constructor
 * @return {ReactElement}
 */
const App: FC = (): ReactElement => {
    return (
        <AppRoute/>
    )
}

export default App;
