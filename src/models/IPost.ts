/**
 * Post model
 * @property {Number} userId
 * @property {Number} id
 * @property {String} title
 * @property {String} Body
 */
export interface IPost {
    userId: number;
    id: number;
    title: string;
    body: string
}